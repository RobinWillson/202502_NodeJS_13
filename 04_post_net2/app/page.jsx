import { Box, AppBar, Typography } from "@mui/material";
const Home = () => {
  return (
    <Box>
      {/* Nav Bar */ }
      <AppBar>
        <Typography variant="h6">POST.NET</Typography>
      </AppBar>
      {/* Main Container */ }
    </Box>
  );
};

export default Home;